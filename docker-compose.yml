version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: ecommerce_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ecommerce_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d ecommerce_db" ]
      interval: 5s
      timeout: 5s
      retries: 5

  mvp-marcketplace-services:
    build: ../mvp-marcketplace-services
    container_name: mvp-marcketplace-services
    volumes:
      - ../mvp-marcketplace-services:/app
      - /app/node_modules
    ports:
      - "3002:3002"
    environment:
      - PORT=3002
      - FAKESTORE_URL=https://fakestoreapi.com
      - VIACEP_URL=https://viacep.com.br/ws
    command: npm run dev

  mvp-marcketplace-api:
    build: ../mvp-marcketplace-api
    container_name: mvp-marcketplace-api
    volumes:
      - ../mvp-marcketplace-api:/app
      - /app/node_modules
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/ecommerce_db?schema=public
      - SECONDARY_API_URL=http://mvp-marcketplace-services:3002
      - PORT=3001
    depends_on:
      db:
        condition: service_healthy
      mvp-marcketplace-services:
        condition: service_started
    command: >
      sh -c "npx prisma db push && npm run dev"

  mvp-front-marcketplace:
    build: .
    container_name: mvp-front-marcketplace
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:3001
    depends_on:
      - mvp-marcketplace-api

volumes:
  postgres_data:
